# Supabase project settings (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only: use only for controlled administrative jobs, never in client code.
SUPABASE_SERVICE_ROLE_KEY=

# Leave empty until the production domain is known. Include https:// when set.
NEXT_PUBLIC_SITE_URL=
